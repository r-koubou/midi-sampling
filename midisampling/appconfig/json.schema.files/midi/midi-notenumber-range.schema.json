{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "description": "Represents the value range of the MIDI note number",
    "properties": {
        "from": {
            "$ref": "midi-notenumber.schema.json"
        },
        "to": {
            "$ref": "midi-notenumber.schema.json"
        }
    },
    "required": [
        "from",
        "to"
    ],
    "examples": [
        { "from": 10, "to": 100 }
    ]
}
