{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Normalize",
    "description": "Structure of the normalize process configuration.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "target_db": {
            "type": "number",
            "description": "The target decibel level"
        }
    },
    "required": [
        "target_db"
    ],
    "examples": [
        {
            "effects":[
                {
                    "index": 0,
                    "name": "normalize",
                    "params": {
                        "target_db": -10.0
                    }
                }
            ]
        }
    ]
}
