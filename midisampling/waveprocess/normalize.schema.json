{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "normalize",
    "description": "Structure of the normalize process configuration.",
    "definitions": {
        "def_normalize_config": {
            "type":"object",
            "additionalProperties": false,
            "properties": {
                "target_db": {
                    "type": "number",
                    "description": "The target decibel level"
                }
            },
            "required": ["target_db"]
        }
    },
    "type": "object",
    "additionalProperties": false,
    "$ref": "#/definitions/def_normalize_config",
    "examples": [
        [
            {
                "target_db": -10.0
            }
        ]
    ]
}
